// This is where the mutation actions would go
// TODO: Create tour, edit tour
import {
} from './actions';

export const initialState = {
  error: null,
};

export default function tours(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
